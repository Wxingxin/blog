### 一、数组解构

#### 1. **基本语法**

```javascript
const arr = [1, 2, 3];
const [a, b, c] = arr;
console.log(a); // 1
console.log(b); // 2
console.log(c); // 3
```

```javascript
const nested = [1, [2, 3], 4];
const [a, [b, c], d] = nested;
console.log(b); // 2
console.log(c); // 3
```

#### 2. **忽略某些值**

```javascript
const [, second, , fourth] = [1, 2, 3, 4];
console.log(second); // 2
console.log(fourth); // 4
```

#### 3. **剩余元素（Rest）**

```javascript
const [first, ...rest] = [1, 2, 3, 4];//rest is array
console.log(first); // 1
console.log(rest); // [2, 3, 4]
```

#### 4. **默认值**

```javascript
const [a = 10, b = 20] = [1];
console.log(a); // 1
console.log(b); // 20（使用默认值）
```

#### 5. **交换变量**

```javascript
let a = 1
let  b = 2;
// console.log(a)报错
// console.log(b) 报错
[a, b] = [b, a];
console.log(a); // 2
console.log(b); // 1
```



### 二、对象解构

#### 1. **基本语法**

```javascript
const person = { name: "John", age: 30 };
const { name, age } = person;
console.log(name); // "John"
console.log(age); // 30
```

```javascript
const user = {
  id: 1,
  info: {
    name: "Alice",
    address: { city: "London" },
  },
};

const {
  info: {
    name,
    address: { city },
  },
} = user;
console.log(name); // "Alice"
console.log(city); // "London"
```

#### 2. **重命名变量**

```javascript
const { name: fullName, age: years } = person;
console.log(fullName); // "John"
console.log(years); // 30
console.log(name)//error
```

#### 3. **默认值**???

```javascript
const { city = "New York" } = person;
console.log(city); // "New York"（如果 person.city 不存在）
```

#### 4. **剩余属性（Rest）**

```javascript
const { name, ...rest } = person;//rest is object
console.log(rest); // { age: 30 }
```

#### 5. **解构函数参数**

```javascript
function printUser({ name, age }) {
  console.log(`${name} is ${age} years old.`);
}

printUser({ name: "Bob", age: 25 }); // "Bob is 25 years old."
```
- 直接解构函数返回的数组或对象：

```javascript
function getPerson() {
  return {
    name: "John",
    age: 30,
    hobbies: ["reading", "coding"],
  };
}

const { name, hobbies } = getPerson();
console.log(hobbies); // ["reading", "coding"]
```
### 三、字符串解构

字符串会被视为字符数组：

```javascript
const [a, b, c] = "abc";
console.log(a); // "a"
console.log(b); // "b"
console.log(c); // "c"
```

### 五、默认值与动态计算

默认值可以是表达式，且会在需要时才计算：

```javascript
const [x = 1 + 2] = [];
console.log(x); // 3

const [y = Math.random()] = [];
console.log(y); // 随机数
```

### 六、结合扩展运算符（Spread）

```javascript
const arr = [1, 2, 3, 4];
const [first, ...rest] = arr;
console.log(rest); // [2, 3, 4]

const obj = { a: 1, b: 2, c: 3 };
const { a, ...others } = obj;
console.log(others); // { b: 2, c: 3 }
```

### 七、注意事项

1. **变量声明与赋值**

   - 解构赋值需要变量声明（`const`, `let`, `var`）或已有变量：

     ```javascript
     // 错误：缺少变量声明
     { x, y } = { x: 1, y: 2 };

     // 正确：用括号包裹
     ({ x, y } = { x: 1, y: 2 });
     ```

2. **对象键与变量名匹配**

   - 对象解构时，变量名必须与对象键名一致（除非重命名）：

     ```javascript
     const { age } = { name: "John", age: 30 };
     console.log(age); // 30

     const { wrongName } = { name: "John" };
     console.log(wrongName); // undefined
     ```

3. **嵌套解构的深度**
   - 解构深度必须与数据结构一致，否则会报错：
     ```javascript
     const {
       info: { address },
     } = { info: null }; // 报错：Cannot destructure 'address' of 'undefined'
     ```

### 八、实际应用场景

1. **配置参数处理**

   ```javascript
   function initApp({ host = "localhost", port = 3000 } = {}) {
     console.log(`Server running at ${host}:${port}`);
   }

   initApp({ port: 8080 }); // "Server running at localhost:8080"
   ```

2. **从模块导入**

   ```javascript
   import { useState, useEffect } from "react";
   ```

3. **遍历对象数组**

   ```javascript
   const users = [
     { id: 1, name: "Alice" },
     { id: 2, name: "Bob" },
   ];

   users.forEach(({ name }) => {
     console.log(name);
   });
   ```

# 💯💯💯 面试题

###  1. 基础概念
**题目：什么是解构赋值？它有什么优势？**  
**答案：**  
解构赋值是 ES6 引入的语法，用于从数组或对象中提取值并赋值给变量。它简化了代码，提高可读性，避免了手动访问属性或索引。  
**示例：**  
```javascript
// 传统方式
const arr = [1, 2, 3];
const first = arr[0];
const second = arr[1];

// 解构
const [a, b] = [1, 2, 3]; // a=1, b=2
const { name, age } = { name: 'Alice', age: 30 }; // name='Alice', age=30
```

**题目：解构赋值支持哪些数据结构？**  
**答案：**  
主要支持数组（Array）和对象（Object），也可以用于 Set、Map 等可迭代对象。数组按位置解构，对象按键名解构。  
**示例：**  
```javascript
const set = new Set([1, 2, 3]);
const [x, y] = set; // x=1, y=2
```

**题目：解构赋值是浅拷贝还是深拷贝？**  
**答案：**  
它是浅拷贝，只复制引用。如果解构的是对象或数组，修改嵌套结构会影响原数据。  
**示例：**  
```javascript
const obj = { nested: { key: 'value' } };
const { nested } = obj;
nested.key = 'new';
console.log(obj.nested.key); // 'new'（原对象被修改）
```

#### 2. 数组解构
**题目：如何在数组解构中跳过元素或使用剩余参数？**  
**答案：**  
使用逗号跳过元素，使用 ...rest 收集剩余元素。  
**示例：**  
```javascript
const [first, , third, ...rest] = [1, 2, 3, 4, 5];
// first=1, third=3, rest=[4,5]
```

**题目：数组解构中如何设置默认值？**  
**答案：**  
在变量后使用 = 默认值，仅当值为 undefined 时生效。  
**示例：**  
```javascript
const [a = 10, b = 20] = [1]; // a=1, b=20
const [c = 30] = []; // c=30
```

**题目：解构空数组或 undefined 会怎样？**  
**答案：**  
解构空数组不会报错，但变量为 undefined；解构 undefined 或 null 会抛出 TypeError。  
**示例：**  
```javascript
const [] = []; // 无变量，无错误
const [x] = []; // x=undefined
// const [y] = null; // TypeError
```

#### 3. 对象解构
**题目：对象解构如何重命名变量？**  
**答案：**  
使用 key: newName 语法重命名。  
**示例：**  
```javascript
const obj = { name: 'Alice', age: 30 };
const { name: userName, age: userAge } = obj; // userName='Alice', userAge=30
```

**题目：对象解构中默认值如何工作？**  
**答案：**  
类似数组，当键不存在或值为 undefined 时，使用默认值。默认值可以是表达式。  
**示例：**  
```javascript
const { name = 'Unknown', age = 0 } = { name: 'Bob' }; // name='Bob', age=0
const { log = console.log } = {}; // log=console.log（默认值是函数）
```

**题目：如何解构不存在的属性？**  
**答案：**  
如果属性不存在，变量为 undefined；可以使用默认值避免。  
**示例：**  
```javascript
const { missing } = { }; // missing=undefined
const { missing = 'default' } = { }; // missing='default'
```

#### 4. 嵌套解构
**题目：如何进行嵌套对象或数组的解构？**  
**答案：**  
在解构中嵌套使用 {} 或 []，并可结合重命名和默认值。  
**示例：**  
```javascript
const obj = { user: { name: 'Alice', address: { city: 'NY' } } };
const { user: { name, address: { city = 'Unknown' } } } = obj; // name='Alice', city='NY'
```

**题目：嵌套解构中如果中间层不存在会怎样？**  
**答案：**  
如果中间层为 undefined，会抛出 TypeError；可以使用默认值处理。  
**示例：**  
```javascript
// const { user: { name } } = { }; // TypeError
const { user: { name = 'default' } = {} } = { }; // name='default'（使用默认空对象）
```

#### 5. 函数参数与解构
**题目：函数参数如何使用解构？**  
**答案：**  
在参数列表中使用解构，简化传入对象或数组的处理，并支持默认值。  
**示例：**  
```javascript
function greet({ name = 'Guest', age }) {
  console.log(`Hello ${name}, age ${age || 'unknown'}`);
}
greet({ name: 'Alice', age: 30 }); // 'Hello Alice, age 30'
greet({ }); // 'Hello Guest, age unknown'
```

**题目：解构在函数参数中的默认值如何与函数默认参数结合？**  
**答案：**  
函数参数可以有整体默认值，解构内也可以有默认值。整体默认先应用。  
**示例：**  
```javascript
function test({ a = 1 } = {}) {
  console.log(a);
}
test(); // 1（整体默认 {}，然后 a 默认 1）
test({ a: 2 }); // 2
```

#### 6. 实际应用与陷阱
**题目：如何使用解构交换变量值？**  
**答案：**  
无需临时变量，直接用数组解构。  
**示例：**  
```javascript
let a = 1, b = 2;
[a, b] = [b, a]; // a=2, b=1
```

**题目：解构在模块导入中的应用？**  
**答案：**  
在 ES 模块中使用命名导入时，本质上是对象解构。  
**示例：**  
```javascript
import { useState, useEffect } from 'react'; // 从 'react' 对象解构
```

**题目：解构的计算属性名如何使用？**  
**答案：**  
对象解构支持 [computedKey]: varName。  
**示例：**  
```javascript
const key = 'name';
const { [key]: userName } = { name: 'Alice' }; // userName='Alice'
```

**题目：字符串或函数返回值的解构？**  
**答案：**  
字符串可作为数组解构（因为可迭代），函数返回值可直接解构。  
**示例：**  
```javascript
const [firstChar] = 'hello'; // firstChar='h'
function getCoords() { return [10, 20]; }
const [x, y] = getCoords(); // x=10, y=20
```

**题目：解构的常见错误是什么？**  
**答案：**  
- 忘记圆括号包裹独立解构语句（因为 {} 被视为块）。  
- 解构 null/undefined 导致 TypeError。  
- 默认值不处理 null（只处理 undefined）。  
**示例：**  
```javascript
// let { a } = { a: 1 }; // 错误，如果独立语句需 (let { a } = { a: 1 });
({ a } = { a: 1 }); // 正确，使用圆括号
const { b = 2 } = { b: null }; // b=null（默认不生效）
```
