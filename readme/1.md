问题的核心就是你这句：

> 只有在 `---` 之内的代码能够显示

这是 **Rspress 的特性**：
当 `pageType: home` 时，这一页会用「首页布局组件」来渲染，**只读取 frontmatter 里的 hero / features 等配置**，下面普通 Markdown 正文是不会显示的。([rspress.rs][1])

所以你写在 `---` 外面的：

```md
- [leetcode](/leetcode/)
## 👋 感谢你翻到这里
...
```

在 `pageType: home` 下是直接被忽略的，这是正常行为。

---

## ✅ 做导航的几种正确方式

### 方式 1：在 hero.actions 里加一个「LeetCode」按钮（最简单）

直接把导航写进首页的大按钮区域：

```yaml
---
pageType: home
hero:
  name: 我的BLOG网站
  text: 一个学习前端的网站
  tagline: This is the tagline
  actions:
    - theme: brand
      text: LeetCode 刷题
      link: /leetcode/    # 指向 docs/leetcode/index.md
    - theme: alt
      text: Quick Start
      link: /guide/
    - theme: alt
      text: GitHub
      link: https://github.com/web-infra-dev/rspress

  image:
    src: /rspress-icon.png
    alt: Logo

features:
  - title: HTML
    details: ...
    icon: 🏃🏻‍♀️
  - title: CSS
    details: ...
    icon: 📦
  # 省略其它 features
---
```

> `/leetcode/` 会自动映射到 `docs/leetcode/index.md`，这是 Rspress 的约定式路由。

---

### 方式 2：在 features 里加一个「LeetCode」卡片

`HomeFeature` 组件的每个 feature 支持 `link` 字段，可以点整块卡片跳转。([v2.rspress.rs][2])

你可以新增一个 feature：

```yaml
features:
  - title: HTML
    details: ...
    icon: 🏃🏻‍♀️

  - title: LeetCode 题解
    details: 刷题记录与解题思路整理。
    icon: 🧠
    link: /leetcode/        # 点击卡片跳转到 LeetCode 文档

  - title: CSS
    details: ...
    icon: 📦
  # ...
```

这样首页 feature 区就会多一个 **LeetCode 卡片**，点了就去 `/leetcode/`。

---

### 方式 3：如果你一定要显示下面那段 Markdown 文本

那就需要 **不用 `pageType: home`** 这种首页布局，而改成普通文档页面或自定义页面：

#### 方案 A：改成普通文档页

```yaml
---
pageType: doc
---
- [leetcode](/leetcode/)

## 👋 感谢你翻到这里
...
```

这样下面的 Markdown 就会按普通文档页面正常显示，但就没有 hero / features 这种首页风格了。

#### 方案 B：自定义页面（高级一点）

用 `pageType: custom`，然后在正文里手动引入 `<HomeHero />`、`<HomeFeature />`，再写你自己的 Markdown 段落（这就稍微进阶一点，如果你想走这条路，我可以给你一份完整 MDX 示例）。

---

## 小结

* `pageType: home` 下：**只渲染 frontmatter**，正文内容会被忽略，这是你现在看到的问题。
* 想在首页做 LeetCode 导航：

  * **最推荐**：在 `hero.actions` 里加 `/leetcode/` 按钮；
  * 或者在 `features` 加一个带 `link: /leetcode/` 的卡片。
* 如果你想保留现在这段「感谢你翻到这里 / 联系方式」的文字，就考虑把首页改为 `pageType: doc` 或做一个 `custom` 页。

如果你告诉我你更喜欢「大按钮跳转」还是「卡片跳转」，我可以按你的那套风格，帮你把完整的 `index.md` 和 `rspress.config.ts` 一整套都整理好。

[1]: https://rspress.rs/guide/default-theme/home-page?utm_source=chatgpt.com "Home page"
[2]: https://v2.rspress.rs/guide/default-theme/components?utm_source=chatgpt.com "Built-in components"
# 
```md
# docs/index.md
features:
  - title: CSS
    details: 功能介绍……
    icon: '<img src="/home/css.png" alt="CSS" width="48" height="48" />'
```