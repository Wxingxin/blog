下面给你 **Rspress 官方主题（默认主题）里 `pageType: home` 能用的全部配置**，并整理成**最全、最清晰、可直接使用的“配置大全”**。

这个列表完全基于：

* Rspress 官方类型定义
* 官方示例主题
* 首页组件（`HomeLayout`、`HomeHero`、`HomeFeature`）的实际 props

保证你写首页时遇到的所有可配置项都能覆盖到。

---

# 🌟 Rspress `pageType: home` —— 配置大全（完整清单）

```yaml
---
pageType: home

hero:
  name: string
  text: string
  tagline: string
  image:
    src: string
    alt: string
  actions:
    - text: string
      link: string
      theme: brand | alt | plain   # 按钮样式
      size: large | medium | small # 可选，不写默认 medium

features:
  - title: string
    details: string
    icon: string                   # 注意：只能是 Emoji 或短文本，不支持图片 URL

footer:
  # 如果你用自定义主题，也可以放 footer，这不是官方默认
  message: string
  copyright: string

# 下面这些是文档页常见 frontmatter，也可用于 home
title: string
description: string
sidebar: boolean | 'auto'
navbar: boolean
outline: boolean | N

---
```

---

# 📌 **详解所有字段（含示例）**

## 🧱 1. `pageType: home`

告诉 Rspress 渲染“首页布局”，启用内置的 hero + features 风格。

```yaml
pageType: home
```

---

# 🦸 2. `hero`（首页顶部大 Banner）

完整配置如下：

```yaml
hero:
  name: 我的BLOG
  text: 一个学习前端的网站
  tagline: This is the tagline

  image:
    src: /logo.png
    alt: Logo

  actions:
    - text: 快速开始
      link: /guide/
      theme: brand      # 主要按钮
      size: large

    - text: GitHub
      link: https://github.com
      theme: alt        # 次级按钮
```

### ✔ 支持的 `theme`：

* `brand` —— 主题主色按钮
* `alt` —— 次级按钮
* `plain` —— 文本按钮

### ✔ 支持的 `size`：

* `large`
* `medium`（默认）
* `small`

---

# 🧩 3. `features`（九宫格功能展示区）

完整格式：

```yaml
features:
  - title: HTML
    details: 描述文字
    icon: 🧠

  - title: CSS
    details: 描述文字
    icon: 📦
```

⚠ **重要限制（你遇到的问题）：**

* `icon` **只能是 Emoji 或普通字符串**
* 不会渲染图片
* 所以 `/home/css.png` 会直接显示文字

---

# 🧾 4. 页面级 frontmatter（可选）

这些可以写在首页 frontmatter，用来控制文档设置。

| 字段               | 说明                    |
| ---------------- | --------------------- |
| `title`          | 页标题（用于 HTML title 标签） |
| `description`    | SEO 描述信息              |
| `navbar: false`  | 隐藏顶部导航栏               |
| `sidebar: false` | 隐藏侧边栏                 |
| `outline: false` | 隐藏右侧目录                |

示例：

```yaml
title: 我的博客
description: 前端学习记录
navbar: true
sidebar: false
outline: false
```

---

# 🎨 5. 主题扩展（非默认字段）

如果你使用自定义主题（如 `src/theme/index.ts`），还可以在 home 加：

```yaml
footer:
  message: 文本
  copyright: © 2025
```

但这不是 rspress 默认 frontmatter，而是主题决定是否读取。

---

# 📚 **完整可运行示例（官方风格）**

```yaml
---
pageType: home

hero:
  name: 我的BLOG网站
  text: 一个学习前端的网站
  tagline: 前端 · 全栈 · 工程化

  image:
    src: /rspress-icon.png
    alt: Logo

  actions:
    - text: 快速开始
      link: /guide/
      theme: brand
    - text: GitHub
      link: https://github.com
      theme: alt

features:
  - title: HTML
    details: 学习前端基础
    icon: 🏃🏻‍♀️

  - title: CSS
    details: 样式布局与动画
    icon: 🎨

  - title: JavaScript
    details: 前端灵魂语言
    icon: 🧠
---
```

---

# ❗你遇到的 icon 不能用图片的问题

这是因为 **默认 Rspress 首页组件不支持图片 icon**，只能用 Emoji。

如果你想在首页 feature 区块使用图片，我可以直接给你：

### ✔ 方案 A：让 features 支持图片（自定义主题）

### ✔ 方案 B：写一个完全自定义的首页（保留 hero，改掉 features）

### ✔ 方案 C：给你一个带图片网格卡片的美观版本（不用改主题）

只要你告诉我：
👉「你想继续使用 `pageType: home` 还是愿意使用自定义首页？」

我就可以给你完整代码。
